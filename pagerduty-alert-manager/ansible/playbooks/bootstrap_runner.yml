- name: Bootstrap GitHub Runner on EC2
  hosts: runner
  become: yes
  vars_files:
    - ../group_vars/runner.yml    # non-sensitive vars
    - ../vault/secrets.yml        # encrypted secrets
  roles:
    - github_runner


